# API：程序之间怎么互相调用

<!-- API 的概念——程序之间的"接口"。用餐厅菜单类比：菜单就是 API——你不需要知道厨房怎么做的，只需要告诉服务员你要什么。讲 RESTful API 的基本思想：URL 定位资源，HTTP 方法操作资源。给出一个实际的 API 调用例子（比如 GitHub API 获取用户信息），让读者看到 API 返回的 JSON 数据。 -->
::: tip 🚧 建设中 / Work in Progress
这篇文章还在打磨中，尚未完成。敬请期待更新！
:::
