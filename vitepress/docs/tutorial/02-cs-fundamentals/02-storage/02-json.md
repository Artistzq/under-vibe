# JSON：数据交换的通用语言

<!-- JSON 格式详解——为什么 JSON 成为数据交换的事实标准（人类可读、语言无关、轻量）。展示基本语法：对象、数组、字符串、数字、布尔值、null。对比 XML 和 YAML（简单提一下区别）。给出实际例子：API 返回的 JSON、配置文件中的 JSON。提示常见错误：多了一个逗号、key 没加引号。 -->

::: tip 🚧 内容建设中
这部分内容正在紧张撰写中，敬请期待！
:::
