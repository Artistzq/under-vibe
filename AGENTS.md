# Under Vibe 代理团队

本文档说明项目中的各个 AI 代理角色，以及如何与它们协作。

---

## ⛳ 写作或改动内容前必读（所有代理）

任何代理在动笔写文章、调整目录、增删页面之前，**必须先读这三个文件**，顺序如下：

| 顺序 | 文件 | 它决定什么 |
|------|------|-----------|
| 1 | [`vitepress/docs/tutorial/plan.md`](./vitepress/docs/tutorial/plan.md) | **预目录 + 进度**：每篇文章的路径、编号、标题、一句话定位、是否已完成 |
| 2 | [`PROJECT_SPEC.md`](./PROJECT_SPEC.md) | 项目定位、目标读者、教学目标、双线课程结构 |
| 3 | [`STYLE_GUIDE.md`](./STYLE_GUIDE.md) | 语气、结构、格式、术语与代码示例规范 |

`plan.md` 是内容的唯一权威目录：

- **新文章**：路径与标题以 `plan.md` 为准，不要自创目录或编号。
- **写完后**：把 `plan.md` 中该篇状态改为 ✅，再在 `vitepress/docs/.vitepress/config.mts` 侧边栏登记链接。
- **新主题**：先写进 `plan.md` 规划，再落笔成文，避免"标题先于内容出现"。
- **不创建占位文件**：规划中但未编写的文章只存在于 `plan.md`，仓库里不放"建设中"空页。

> ⚠️ 没读 `plan.md` 就新增或移动文档，视为无效改动，质量验收员会直接打回。

---

## 🎯 团队概览

| 代理 | 类型 | 快捷键 | 职责 |
|------|------|--------|------|
| 🏗️ 文档站点架构师 | 主代理 | `@site-architect` | 框架搭建、功能开发 |
| 📝 内容创作助手 | 主代理 | `@content-writer` | 你日常写内容的主力伙伴 |
| ✅ 质量验收员 | 子代理 | `@quality-assurance` | 所有产出的质量守门员 |

---

## 🚀 如何使用

### 切换主代理
在 opencode 界面按 **Tab 键**，可以在主代理之间切换：
- `@site-architect` - 当你想改框架、加功能时
- `@content-writer` - 当你想写新文章、改内容时（日常默认用这个）

### 提及子代理
任何时候你都可以在对话中 `@质量验收员` 来让它检查当前内容。

---

## 🔄 标准工作流程

### 场景 1：写新内容
```
你 @content-writer：帮我写一篇"端口被占用怎么办"的文章

    ↓
内容创作助手写完草稿

    ↓
内容创作助手 @质量验收员：请验收这篇内容

    ↓
质量验收员：验收通过 / 有这些问题需要修改

    ↓
交付给你
```

### 场景 2：修改框架
```
你 @site-architect：我想加一个全文搜索功能

    ↓
架构师设计方案并实现

    ↓
架构师 @质量验收员：请验收搜索功能

    ↓
质量验收员：验收通过 / 有这些问题

    ↓
交付给你
```

### 场景 3：交叉验证
```
你：这个页面显示有点问题

@site-architect：我看一下... 修复了

@quality-assurance：请验收修复

质量验收员：确认修复，验收通过
```

---

## 📝 内容创作流程（你需要知道的）

**使用 @content-writer 时，你只需要：**

1. **描述你想写什么**
   > "帮我写一篇关于 Git 的文章，从初学者角度，讲清楚 commit 到底是什么"

2. **确认大纲**
   > 内容助手会先给你一个大纲，确认方向对不对

3. **审阅内容**
   > 内容助手写完后，你可以直接说："这里讲得太复杂了，改简单点"

**其他的事情它会帮你处理：**
- ✅ 自动套用正确的页面模板
- ✅ 自动更新侧边栏导航
- ✅ 自动加"上一章/下一章"链接
- ✅ 自动维护术语表
- ✅ 符合风格规范

---

## 🏗️ 框架开发流程（你需要知道的）

**使用 @site-architect 时，你可以：**

1. **提需求**
   > "我想加一个评论功能，每个文章底部可以留言"
   > "侧边栏能不能加个目录，跳转到文章内的标题"

2. **它负责**
   - 设计方案
   - 实现代码
   - 调用质量验收员
   - 验收通过交付给你

---

## ✅ 质量验收标准

质量验收员是团队的"黑脸"，它的标准很严格：

### 框架验收
- 构建脚本不报错
- 所有页面导航一致
- 所有链接能正常跳转
- 移动端显示正常
- 样式美观符合设计

### 内容验收
- 符合 STYLE_GUIDE.md 风格
- 术语定义清晰统一
- 步骤清晰可操作
- 导航链接正确

---

## 💡 最佳实践

### 1. 选对代理
- **写内容** → 找 @content-writer
- **改框架** → 找 @site-architect
- **查问题** → 直接 @quality-assurance

### 2. 一次只做一件事
不要同时说"帮我写一篇文章，顺便把搜索功能加了"。分开说，一个一个来。

### 3. 充分交流
内容写得不对就直接说，不要怕麻烦。AI 不会不耐烦。

### 4. 相信验收员
如果验收员说有问题，那一定是有问题。不要跳过验收环节。

---

## 📌 代理配置文件位置

所有代理的配置文件都在：
```
.opencode/agents/
├── site-architect.md      # 文档站点架构师
├── content-writer.md      # 内容创作助手
└── quality-assurance.md   # 质量验收员
```

你可以随时修改这些文件来调整代理的行为。修改后，opencode 会自动加载最新配置。
