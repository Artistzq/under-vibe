# Under Vibe 项目规范

## 项目定位

Under Vibe 不只是计算机通识课，也不只是"人工接管"训练。它有三个目标：

1. **让零基础用户理解 AI 正在做什么**——看懂 AI 给出的命令、文件、项目结构和技术方案，理解"为什么要这么做"，而不是只会复制粘贴。
2. **让用户最终做出一个真实项目**——AI 可以参与甚至完成大量代码工作，读者不需要手写所有代码，但必须逐步理解项目是如何构成、运行和演化的。课程不能只停留在知识介绍，必须有实际项目产出。
3. **让用户具备 AI 失效时的人工接管能力**——AI 卡住、误解需求、反复修改或做出危险操作时，用户知道发生了什么，能够判断项目状态、读取错误、设计排查步骤、验证结果、回退修改；在涉及删除、数据、权限、密钥和部署等高风险操作时，能够人工接管。

> **核心定位**：Under Vibe 帮助原本不会写代码、但想使用 VibeCoding 的人，理解 AI 的工作过程，和 AI 一起做出项目，并在 AI 失效时具备基本的判断和接管能力。

## 目标读者

- 完全零基础的普通人
- 只会用 GUI 工具，几乎不懂命令行的人
- 做过一些 vibecoding 项目但经常卡住、不知道该了解什么的人

## 教学理念

AI 时代不应再以"所有代码都手写"为唯一学习方式。更适合的教学方式是：

```text
AI 负责生成和执行
人负责理解目标、理解过程、判断结果和控制风险
```

课程同时培养：项目产出能力、计算机基础理解、AI 协作能力、问题排查能力、人工接管能力。不能只教"怎么做"，还要解释：为什么这么做、做完以后产生了什么、哪些文件发生了变化、哪个程序在运行、数据去了哪里、出错时应该观察什么、AI 的方案是否可信。

## 教学目标（核心能力）

课程最终应让读者具备：

1. 理解 AI 的操作
2. 理解项目当前状态
3. 理解一个动作会产生什么影响
4. 看懂项目的基本结构
5. 知道程序为什么能运行
6. 看懂错误信息并进行分类
7. 设计最小排查步骤
8. 判断 AI 方案是否可信
9. 验证项目是否真正完成目标
10. 保存、回退和恢复项目
11. 在 AI 卡住时继续推进
12. 最终和 AI 一起完成并维护一个项目

> 不是把读者训练成脱离 AI 的程序员，而是把读者训练成能够驾驭 AI、理解项目并对最终结果负责的项目建设者。

## 双线课程结构

课程采用两条同时推进的线：

### 主线：完成一个真实项目

主线围绕一个贯穿项目展开：

```text
提出需求 → 创建项目 → 配置环境 → 安装依赖 → 启动项目
→ 修改功能 → 浏览器与服务器通信 → 保存数据
→ 处理错误 → 使用 Git 保存版本 → 部署和分享
```

主线按阶段组织，目录顺序体现学习顺序而非学科分类，具体目录见 `vitepress/docs/tutorial/plan.md`（预目录）。

### 理论线：解释项目中遇到的计算机知识

理论不按传统学科顺序孤立展开，而是由主线中的问题引出：

```text
AI 执行 npm install       → 命令、终端、网络、依赖、文件系统
AI 启动 npm run dev       → 进程、运行时、端口、环境变量
浏览器访问 localhost      → HTTP、客户端、服务端、DNS、API
项目需要保存数据          → JSON、文件、数据库、缓存、数据结构
AI 遇到报错              → 错误信息、执行流程、状态、日志、调试
项目需要上线              → 构建、部署、服务器、权限、Git、回滚
```

理论必须回答"它解释了主线中的哪个现象"；暂时无法对应主线的知识放入支线知识库（`side-topics/`），不打断主线。

### 理解实验专题（Build Lab）

不在每篇文章中加入完整实验。每个阶段学习完后，设置一个集中实验专题，读者关掉 AI 手动完成（如"手动启动一个项目"：创建目录、查看文件、检查运行时、安装依赖、启动、停止进程、解释每一步产生的变化）。实验的目的不是让用户以后不用 AI，而是形成完整的系统地图：

```text
理论文章 → 阶段总结 → Build Lab 手动实验 → 进入下一阶段
```

### 主线与支线

- **高亮块**：放即时需要知道的内容——简短术语解释、当前操作的安全提醒、暂时不深入的概念、与当前文章直接相关的小补充。
- **主线文章**：放完成项目必须理解的内容——终端、路径和文件系统、环境和依赖、项目结构、HTTP 和客户端/服务端、数据存储、错误与调试、Git、部署和回滚。
- **支线文章**：放完整的计算机通识——操作系统、进程和内存、计算机组成原理、编译原理、数据结构、离散数学、网络底层细节。

主线文章可以提供"想深入了解"的链接，但不要强制读者立即离开主线。

## 文件职责

- `PROJECT_SPEC.md`：项目定位、目标读者、教学目标、双线课程结构
- `STYLE_GUIDE.md`：文章语气、结构、格式、术语和代码示例规范
- `vitepress/docs/tutorial/plan.md`：文章目录、学习顺序、文章状态（进度维护）
- `README.md`：项目介绍、本地运行、贡献方式
- `AGENTS.md`：AI 代理角色分工，以及"写作前必读 `plan.md`"的强制约定

项目进度维护在 `plan.md` 中，不在本文件维护进度/待办清单。

**写作入口约定**：任何写作者（人或 AI 代理）动笔前须先读 `vitepress/docs/tutorial/plan.md`（预目录 + 进度），再读本文件与 `STYLE_GUIDE.md`。该约定同时写在 `AGENTS.md`、`README.md` 以及 `.opencode/agents/*.md` 的代理提示词中，保证从任一入口进入项目都会被引导到 `plan.md`。

## 未来交互功能（展望）

"观察 AI 做了什么"和"人工接管"可作为后续产品能力，目前不进入实现。未来可单独开发前端组件和登录系统，提供：AI 操作过程回放、文件修改前后对比、命令解释、用户回答 AI 做了什么、故意注入错误、用户执行接管流程、保存学习进度、阶段任务和实验记录。当前阶段先建立文章结构和教学逻辑，不开发这部分功能。
